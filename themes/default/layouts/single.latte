{layout 'base.latte'}

{block title}{$page->title} - {$site->name}{/block}
{block description}{$page->excerpt ?? ''}{/block}

{block content}
<div class="container">
    <div class="content-area">
        <article class="post">
            <header class="post-header">
                <h1 class="post-title">{$page->title}</h1>
                <div class="post-meta">
                    {if $page->author->name}
                        <span class="author">{$page->author->name}</span>
                    {/if}
                    {if $page->publishedAt}
                        <time datetime="{$page->publishedAt}">{$page->publishedAt|date:'F j, Y'}</time>
                    {/if}
                </div>
            </header>

            <div class="post-body">
                {$page->renderedBody|noescape}
            </div>

            <footer class="post-footer">
                {=slot('after_content')|noescape}
            </footer>
        </article>
    </div>

    <aside class="sidebar">
        {=slot('sidebar')|noescape}
    </aside>
</div>
{/block}
